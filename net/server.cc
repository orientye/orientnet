#include "net/server.h"

Server::Server() {}
Server::~Server() {}

int Server::start() { return 0; }
int Server::stop() { return 0; }
int Server::join() { return 0; }

int Server::add_service() { return 0; }
int Server::remove_service() { return 0; }
int Server::clear_services() { return 0; }
